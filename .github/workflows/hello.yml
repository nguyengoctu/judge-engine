on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello, World!"
      - name: branches
        run: echo "${{ github.ref_name }}, ${{ github.sha }}"